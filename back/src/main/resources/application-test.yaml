spring:
  datasource:
    url: jdbc:h2:mem:db_test;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
#    hikari:
#      maximum-pool-size: 2 # 커넥션 풀 개수를 2개로 제한
#      connection-timeout: 1000 # 커넥션 획득 대기 타임아웃을 1초로 설정
custom:
  cors:
    allowed-origins: http://localhost:3000